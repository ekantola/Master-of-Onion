/* Objflags:
-Wall -Werror -O -g
*/


#include "misc.h"

#include <stdio.h>


/* Find a string in a list
int find_str(char *str, char *list[], int length)
{
  int i;

  if(str != null)
    for(i=0; i<length; ++i)
      if(strcmp(list[i], str) == 0)
        return i;  // found at index i

  return -1;  // not found
}
*/
